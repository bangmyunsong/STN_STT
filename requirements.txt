# Render 512MB 극한 최적화 - Whisper tiny 모델 전용
# 메모리 사용량을 절대 최소로 제한

# FastAPI 핵심 (고정 버전)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
pydantic==2.5.0

# 기본 유틸리티 (최소)
python-dotenv
requests==2.31.0
psutil

# 극경량 PyTorch CPU 전용 (안정 버전)
torch==2.1.0
torchaudio==2.1.0

# Whisper tiny 모델 (필수)
openai-whisper

# 오디오 처리 (최소)
pydub

# ERP 추출용 (선택적)
openai==1.3.0

# 🚫 메모리 절약을 위해 제외:
# pyannote-audio - 화자분리 (메모리 과다 사용)
# APScheduler - 스케줄러 기능 비활성화
# supabase - 데이터베이스 기능 비활성화  
# soundfile - pydub로 대체
# matplotlib, pandas, plotly - 분석/시각화 제거