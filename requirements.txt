# Render 무료 플랜용 초경량화 requirements (Tiny 모델)
# 512MB 메모리 제한 하에서 작동하도록 최적화

# FastAPI 핵심만 (필수)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart
pydantic>=2.0.0

# 기본 유틸리티 (최소한)
python-dotenv
requests>=2.31.0

# Whisper 최소 의존성 (PyTorch 제외하고 CPU 추론만)
openai-whisper

# 오디오 처리 (최소한)
soundfile
pydub

# API 통신만 (필수)
openai>=1.0.0

# 스케줄러 제외 (메모리 절약)
# APScheduler>=3.10.0

# Supabase 제외 (메모리 절약) - DB 기능 비활성화
# supabase>=2.0.0